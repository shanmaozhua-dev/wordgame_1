# 勇者试炼复刻交接文档

## 本项目定位

本项目是从 `E:\wordgame copy\新建游戏项目` 复制出的独立 Godot 4.7 项目，目标是复刻《文字游戏》第三章“添譜來堂 / 拳頭”里的勇者试炼片段。

当前不是旧游戏源码迁移，也不复制旧项目素材；它保留前一个框架的网格玩法能力，再新增真实关卡数据和自动测试。

## 今天新增内容

- 新项目目录：`E:\wordgame copy\勇者试炼`。
- 默认运行 `Main.tscn` 会加载 `LevelLoader.build_hero_trial_fist_level()`。
- 新增 `tests/test_hero_trial_fist.gd`，自动验证：
  - 玩家初始在右下手掌通道附近。
  - `贏/不/二/讚/一/零/劍/勇` 等初始关键字存在，且可推/可删属性正确；`好` 在原源码里有条件显示，初始不生成。
  - 上方叙述和下方手势句都是地图文字，有碰撞。
  - 测试中临时生成 `好` 并放入 `巨大手掌，是＿的手勢` 的空位后，识别 `巨大手掌，是好的手勢`，设置 `ch3_好的手勢成立` 和 `current_gesture=好`。
  - 识别后生成 `已識別：好的手勢` 和 `好手勢`，二者都是有碰撞的地图文字。
  - 删除 `不` 后形成 `會輕易放開`，设置 `ch3_會輕易放開成立` 与 `hero_trial_complete`；当前不再生成任何杜撰的尾声地图文字。
- `GridWorld` 新增轻量状态接口：
  - `switches`: 存关卡开关，例如 `ch3_好的手勢成立`。
  - `state`: 存当前关卡状态，例如 `current_gesture`。
  - `map_text_lines`: 把一行文字拆成逐字实体，适合复刻截图里的固定地图文字。
  - `entity_spawns`: 显式摆放可推、可删、NPC、道具等单字/多字实体。
  - `sentence_rules[*].spawn_entities`: 造句成功后向地图生成持久文字。
- 根据原版第二张截图测算，一屏固定为 `32 x 18` 个文字格。
- 当前勇者试炼关卡渲染格为 24px，目的是让完整 32 x 18 页面进入自动截图窗口；玩法坐标仍然全部按文字格记录。
- `Fonts/Zpix.tres` 已取消 `fixed_size = 54`，否则 Label 的字号覆盖不会生效，字会互相挤压。
- 手掌墙已改为从原版 `15_添譜來堂_拳頭.tscn` 的 `零的手勢.big_text`、`生命線.big_text`、`拇指下收.big_text` 逐格还原，并在测试里锁定所有初始可见 `palm_wall` 坐标。
- 新增 `tools/extract_tscn_big_text.py`，后续复刻其他手势或关卡时用它提取 `.tscn` 内 `big_text` 的绝对坐标。
- `levels/hero_trial_fist_scene_01.json` 和 `levels/hero_trial_fist_scene_02.json` 是手动编辑器保存的场景参考稿。
- `levels/hero_trial_fist_state_life_line_without_good.json` 是“我”把 `逼退好手的生命線` 里的 `好` 移出之后的同一关卡状态，不是第三张独立场景。这个状态里不应该再有单独的 `好` 实体；`tests/test_map_editor_io.gd` 已经加入回归检查。
- 新增 `scripts/hero_trial_flow.gd` 和 `tests/test_hero_trial_flow.gd`：运行时先加载 `hero_trial_fist_scene_01.json`；按 Space 后加载 `hero_trial_fist_scene_02.json`；当句中 `好` 被推或拉离 `(14,13)` 后，底图切换到 `hero_trial_fist_state_life_line_without_good.json`，但这个被移动的 `好` 会保留在它当前所在格子，且继续可推。
- 新增 `levels/hero_trial_fist_state_one_gesture.json`：这是 `一` 进入底部 `巨大手掌，是＿的手勢` 字位后的静态手掌状态。文件里移除了截图中动态移动出来的 `零` 和 `好`，运行时由 `HeroTrialFlow` 按实际位置加回，避免把某一次手动截图的位置写死。
- 新增 `levels/hero_trial_fist_state_zero_gesture.json`：这是 `零` 回到底部手势字位后恢复的静态零手势状态。文件基于 `hero_trial_fist_scene_02.json`，但移除了动态 `一` 和生命线里的 `好`；切换时 `HeroTrialFlow` 会把当前地图上的 `一`、`好` 按实际位置加回。
- `GridWorld.move_entity_by()` 支持可推动字的链式推动：例如玩家推 `一` 挤向 `零` 时，会先把 `零` 往同方向推出一格，再让 `一` 进入原 `零` 的字位。不可推动字和墙仍然阻挡。

## 勇者试炼参考点

本次确认复刻对象是：

- `E:\文字游戏\文字遊戲.7z\文字游戏源码\Scenes\Maps\第三章\14_添譜來堂_拳頭轉場.tscn`
- `E:\文字游戏\文字遊戲.7z\文字游戏源码\Scenes\Maps\第三章\15_添譜來堂_拳頭.tscn`
- `E:\文字游戏\文字遊戲.7z\文字游戏源码\Scenes\Maps\第三章\16_添譜來堂_尾聲.tscn`

当前重点参考 `15_添譜來堂_拳頭.tscn`：

- 句规：`巨大手掌，是好的手勢`、`巨大手掌，是二的手勢`、`巨大手掌，是讚的手勢`、`巨大手掌，是贏的手勢`、`會輕易放開`、`不會輕易放開`。
- 初始字位置：`贏(1,2)`、`不(6,3)`、`二(1,5)`、`讚(10,5)`、`一(18,17)`、`零(26,17)`、`劍(16,7)`、`勇(24,4)`。源码里的 `好` 有 `exist_condition = "s:ch3_生命線敘述出現==true"`，所以首屏初始不放；`(14,13)` 属于 `拇指下收` 手掌墙。
- 首屏截图显示：左上叙述、右侧巨大手掌、掌心 `劍`、右侧通道里的 `勇`、底部手势句。

还需要更多素材才能继续 1:1：

- 每个手势成立后的手掌变化截图。
- 失败选择或错误句规触发后的反馈。
- 尾声 `16_添譜來堂_尾聲` 的完整画面与对话。

## 继承自框架的能力

## 今天完成了什么

- 在干净 Godot 4.7 项目里搭了一个轻量玩法框架。
- 玩家操纵目标文字 `我`，按格移动；一屏固定为 32x18 个文字格，具体像素大小由关卡渲染配置决定。
- 所有文字实体都有格子碰撞，玩家不会和字重叠。
- 实现了屏幕页切换：逻辑地图固定，玩家跨出 32x18 当前页后，显示层一次性平移到下一页。
- 实现并测试了空格互动、退格删字、推字、Alt 拉字、Tab 拆字、组字、造句识别和高亮。
- 明确了 Alt 拉字规则：按住 Alt 时只能朝远离被拉文字的方向移动。比如 `我` 在字上方，只能 Alt + 上；Alt + 左、右、下都不允许，除非松开 Alt。
- 明确了自动合字规则：不是调用测试函数手动合成，而是玩家推着一个偏旁往另一个偏旁上挤时自动合成。例如推 `又` 撞 `戈`，生成 `戏`。
- 明确了造句提示规则：`已识别` 这类提示会生成成地图上的文字实体，有碰撞，后续也可以给其中某些字补交互。
- 明确了普通提示规则：例如 `手表可以查看人类世界的时间` 也会作为地图文字实体出现，不自动消失，默认有碰撞。重复触发同一句提示时复用已有实体，不刷出一堆重复提示。
- 增加了 F5 自动演示路线，方便组里快速看机制。
- 增加了 headless 自动测试，方便后续重构时回归。
- 增加了 `tools/run_all_tests.ps1` 一键验证脚本，包含自动打开 Godot、截图、保存画面和基础像素检查。
- 画面字体已切换为 `Fonts/Zpix.ttf`，视觉更接近原版。
- 新增 `docs/测试复用指南.md`，后续按截图继续做关卡时优先看这份。

## 本地参考资料

不要把下面这些目录复制进 GitHub 仓库，只作为本机参考：

- 原游戏解包：`E:\文字游戏\文字遊戲.7z\文字遊戲`
- 迁移尝试源码：`E:\文字游戏\文字遊戲.7z\文字游戏源码`

本次主要参考的旧源码思路：

- `Scripts\BaseCharacter.gd`：网格移动、碰撞、可推动对象。
- `Scripts\Player.gd`：输入、推、拉、退格、Tab 拆字。
- `Scripts\Event.gd`：单字事件、删除、拆分、合并。
- `Scripts\MainMap.gd`：地图文字数组、造句规则、高亮。
- `Scripts\Camera.gd`：grid snapping 页式镜头。

## 关卡数据格式

当前测试关卡在 `scripts/level_loader.gd`。

`rows` 是文本网格：

- 一个中文字符默认占一个格子。
- 空格表示空地。
- `我` 表示说明上的玩家起点，但真实起点以 `player_start` 为准。
- 多字词例如 `手表` 会作为一个实体解析，占连续两个格子。

`entities` 用来覆盖默认属性：

```gdscript
"石": {"pushable": true, "solid": true}
"删": {"deletable": true, "solid": true}
"手表": {"interact_text": "手表可以查看人类世界的时间", "solid": true}
```

规则配置：

```gdscript
"split_rules": {"戏": ["又", "戈"]}
"merge_rules": {"又+戈": "戏", "戈+又": "戏"}
"sentence_rules": {"天气": {"message": "已识别"}}
```

当 `sentence_rules` 识别成功时，`message` 不只是 UI 字幕，还会生成地图文字实体。默认生成在被识别词附近的空格上，且 `solid=true`。后续可以扩展：

```gdscript
"sentence_rules": {
	"天气": {
		"message": "已识别",
		"caption_pos": Vector2i(16, 9),
		"caption_interact_text": "这里可以添加特殊交互"
	}
}
```

普通交互提示同理：`interact_text` 会调用同一套地图提示生成逻辑，所以可以直接用于测试提示文字碰撞。后续如果需要让提示文字可删、可推或可互动，可以继续扩展提示配置。

后续让 Codex 做关卡时，推荐输入：

1. 原游戏截图。
2. 这张图对应的文字地图草稿。
3. 哪些字可推、可删、可拆、可互动。
4. 本关拆字、合字、造句规则。
5. 目标演示路线。

## 运行和测试

打开 Godot 4.7，运行 `Main.tscn`。

控制：

- 方向键：移动。
- Space：互动。
- Backspace：删字。
- Tab：拆字。
- Alt + 方向键：拉字。
- F5：自动演示。

命令行测试：

```powershell
.\tools\run_all_tests.ps1
E:\Godot\Godot_v4.7-stable_win64_console.exe --headless --path "E:\wordgame copy\勇者试炼" -s res://tests/test_gameplay_core.gd
E:\Godot\Godot_v4.7-stable_win64_console.exe --headless --path "E:\wordgame copy\勇者试炼" -s res://tests/test_hero_trial_fist.gd
E:\Godot\Godot_v4.7-stable_win64_console.exe --headless --path "E:\wordgame copy\勇者试炼" -s res://tests/test_precision_movement.gd
```

## 后续建议

- 先保留 `GridWorld` 作为唯一玩法真相来源，输入、演示和测试都调用它。
- 地图规模变大后，把 `LevelLoader.build_test_level()` 拆成多个关卡资源或 JSON 文件。
- 如果要更像原作，再单独做文字渲染层；不要把旧项目的大量全局单例直接搬进来。
- 如果中文显示异常，优先加入可分发的开源 CJK 字体，不复制原游戏字体或素材。
- 每做一个新机制，先在 `tests/test_gameplay_core.gd` 增加回归测试。
